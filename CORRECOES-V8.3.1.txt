CbOfertas V8.3.1 — correção de compilação

Erro corrigido:
screenContainsAny(AccessibilityNodeInfo, String...) is already defined

Causa:
O método screenContainsAny foi incluído duas vezes no
WhatsAppAutomationService.java.

Correção:
- Mantida apenas uma implementação do método.
- Versão atualizada para 8.3.1 / 831.
- Fluxo de envio único e retorno ao CbOfertas preservado.
