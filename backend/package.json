{
  "name": "cbofertas-backend-v52",
  "version": "5.2.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && find src -name \"*.js\" -print0 | xargs -0 -n1 node --check",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "description": "Backend modular CbOfertas V5.2 com parser de preço promocional e links meli.la e Shopee"
}
