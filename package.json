{
  "name": "cbofertas-v6-workspace",
  "version": "6.0.0-alpha.3",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-project.mjs && npm --prefix backend run validate",
    "test": "npm --prefix backend test"
  }
}
